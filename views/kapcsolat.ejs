<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Kapcsolat – Rádióadók</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<%- include('partials/navbar', { activePage: 'kapcsolat' }) %>

<main class="container my-5">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="h3 mb-4">Kapcsolatfelvétel</h1>

          <% if (hiba) { %>
            <div class="alert alert-danger"><%= hiba %></div>
          <% } %>

          <% if (siker) { %>
            <div class="alert alert-success"><%= siker %></div>
          <% } %>

          <form action="/kapcsolat" method="post">
            <div class="mb-3">
              <label class="form-label">Név</label>
              <input
                type="text"
                class="form-control"
                name="kuldoNev"
                value="<%= kuldoNev || '' %>"
                required>
            </div>

            <div class="mb-3">
              <label class="form-label">E-mail cím</label>
              <input
                type="email"
                class="form-control"
                name="kuldoEmail"
                value="<%= kuldoEmail || '' %>"
                required>
            </div>

            <div class="mb-3">
              <label class="form-label">Tárgy</label>
              <input
                type="text"
                class="form-control"
                name="targy"
                value="<%= targy || '' %>"
                required>
            </div>

            <div class="mb-3">
              <label class="form-label">Üzenet</label>
              <textarea
                class="form-control"
                rows="4"
                name="szoveg"
                required><%= szoveg || '' %></textarea>
            </div>

            <div class="d-flex justify-content-between align-items-center">
              <a href="/" class="btn btn-outline-secondary">
                Mégse
              </a>
              <button type="submit" class="btn btn-primary">
                Üzenet elküldése
              </button>
            </div>

          </form>
        </div>
      </div>
    </div>
  </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
