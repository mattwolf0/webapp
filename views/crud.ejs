<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>CRUD – Rádióadók</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<%- include('partials/navbar', { activePage: 'crud' }) %>

<main class="container my-5">

  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="h2 mb-1">Rádióadók (CRUD)</h1>
      <p class="text-muted mb-0">
        Itt tudod karbantartani az adatbázisban lévő rádióadókat.
      </p>
    </div>
    <a href="/crud/new" class="btn btn-primary">
      Új rádióadó
    </a>
  </div>

  <% if (message) { %>
    <div class="alert alert-success"><%= message %></div>
  <% } %>

  <% const hasAdok = Array.isArray(adok) && adok.length > 0; %>

  <% if (hasAdok) { %>
    <div class="card shadow-sm">
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-striped table-hover align-middle mb-0">
            <thead class="table-dark">
            <tr>
              <th>ID</th>
              <th>Frekvencia (MHz)</th>
              <th>Teljesítmény (kW)</th>
              <th>Csatorna</th>
              <th>Adóhely / cím</th>
              <th>Település</th>
              <th>Műveletek</th>
            </tr>
            </thead>
            <tbody>
            <% adok.forEach(function(ado) { %>
              <tr>
                <td><%= ado.id %></td>
                <td><%= ado.frekvencia %></td>
                <td><%= ado.teljesitmeny %></td>
                <td><%= ado.csatorna %></td>
                <td><%= ado.cim %></td>
                <td><%= ado.telepules && ado.telepules.nev ? ado.telepules.nev : '' %></td>
                <td>
                  <div class="btn-group btn-group-sm" role="group">
                    <a href="/crud/edit/<%= ado.id %>"
                       class="btn btn-outline-secondary">Szerkesztés</a>
                    <a href="/crud/delete/<%= ado.id %>"
                       class="btn btn-outline-danger"
                       onclick="return confirm('Biztosan törölni szeretnéd ezt a rádióadót?');">
                      Törlés
                    </a>
                  </div>
                </td>
              </tr>
            <% }); %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  <% else { %>
    <p class="text-muted">
      Még nincs egyetlen rádióadó sem az adatbázisban.
    </p>
  <% } %>

</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
