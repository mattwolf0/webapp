<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <title>Adatbázis – Rádióadók</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet">
</head>
<body>

<%- include('partials/navbar', { activePage: 'adatbazis' }) %>

<main class="container my-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="h2 mb-1">Rádióadók adatbázisa</h1>
      <p class="text-muted mb-0">
        Az összes rádióadó listája az adatbázisból.
      </p>
    </div>
    <a href="/crud" class="btn btn-outline-primary btn-sm">
      Adatok szerkesztése (CRUD)
    </a>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <div class="table-responsive">
        <table class="table table-striped table-hover align-middle mb-0">
          <thead class="table-dark">
          <tr>
            <th>ID</th>
            <th>Frekvencia (MHz)</th>
            <th>Teljesítmény (kW)</th>
            <th>Csatorna</th>
            <th>Adóhely / cím</th>
            <th>Település</th>
            <th>Megye</th>
            <th>Régió</th>
          </tr>
          </thead>
          <tbody>
          <% if (Array.isArray(adok) && adok.length > 0) { %>
            <% adok.forEach(function(ado) { %>
              <tr>
                <td><%= ado.id %></td>
                <td><%= ado.frekvencia %></td>
                <td><%= ado.teljesitmeny %></td>
                <td><%= ado.csatorna %></td>
                <td><%= ado.cim %></td>
                <td><%= ado.telepules && ado.telepules.nev ? ado.telepules.nev : '' %></td>
                <td><%= ado.telepules && ado.telepules.megye ? ado.telepules.megye.nev : '' %></td>
                <td><%= ado.telepules && ado.telepules.megye && ado.telepules.megye.regio
                        ? ado.telepules.megye.regio.nev
                        : '' %></td>
              </tr>
            <% }); %>
          <% else { %>
            <tr>
              <td colspan="8" class="text-muted">
                Nincs megjeleníthető rádióadó.
              </td>
            </tr>
          <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
